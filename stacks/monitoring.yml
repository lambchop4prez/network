---
services:
  gatus:
    image: twinproduction/gatus:latest
    container_name: gatus
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - /storage/gatus/config:/config
      - /storage/gatus/data:/data
    environment:
      - GATUS_CONFIG_PATH=/config/config.yaml

networks:
  default:
    name: monitoring_default
