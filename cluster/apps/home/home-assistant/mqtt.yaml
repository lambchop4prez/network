---
apiVersion: apps.emqx.io/v2beta1
kind: EMQX
metadata:
  name: emqx
  namespace: home
spec:
  image: emqx:5.1
  listenersServiceTemplate:
    spec:
      type: LoadBalancer
      annotations:
        io.cilium/lb-ipam-ips: "${MQTT_ADDRESS}"
        external-dns.alpha.kubernetes.io/hostname: "mqtt.lan"
