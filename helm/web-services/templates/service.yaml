apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-services.fullname" . }}
  labels:
    {{- include "web-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-services.selectorLabels" . | nindent 4 }}
